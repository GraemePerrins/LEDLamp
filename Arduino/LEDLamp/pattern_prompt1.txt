you are an expert arduino developer using the FastLED library.

you have a LED strip of 60 leds. you need to create a arduino code for a pattern.ino file that contains two functions:

void PatternInit(int numLeds);
void PatternCycle();

the PatternInit function is called once at the start to setup any state variables needed for the main pattern code. All global variables should be prefixed with _pat1 and all global constants with _PAT1_ to ensure namespace uniqueness and the resulting names be in camel case.

the PatternCycle function is called every 20ms and on each call should advance the pattern cycle on the leds using the FastLed library and using HSV colors. 

create a Pattern function which creates a spectrum from deep pink, dark purple to blue that oscilates back and forth across the LED strip with each color is spread over approx 20 leds. There should be a smooth transition between colors. The oscilation of the colors across the strip should take 5 seconds.

think hard about this and check the the code has no syntax errors and is correct.


